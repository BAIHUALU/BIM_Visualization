# BIM 施工进度可视化系统需求文档

## 1. 项目目标

本项目用于深圳宝安国际机场 T2 航站楼基础工程的 BIM 施工进度可视化。系统以 `Airport1.glb` 作为数字基座，以施工单位提供的 `schedule_data.json` 作为官方计划来源，以人工标定得到的 `zone_definitions.json` 作为施工区域边界来源。

正式版系统不再沿用 demo 中“单构件级进度”的思路，而是采用板块级进度：

```text
施工板块 = 大区 + 展示分类
```

示例：

```text
E1 + 管桩
E1 + 钢板桩
W3 + 钢支撑
E12 + 咬合桩
```

系统目标是让用户通过拖动时间轴，查看不同施工板块在计划进度下的状态变化，并能点击模型查看对应板块信息。

## 2. 核心数据来源

正式项目依赖三个源文件：

```text
Airport1.glb
schedule_data.json
zone_definitions.json
```

`Airport1.glb`：模型数字基座，提供构件几何位置、构件名称、ElementID、部分 Revit 参数。

`schedule_data.json`：官方进度计划来源，提供区域、任务名称、任务类型、计划开始日期、计划结束日期。

`zone_definitions.json`：区域标定成果，记录 E1-E13、W1-W6 在模型坐标系中的多边形边界。

## 3. 业务范围

第一版系统只做计划进度可视化，不做实际进度录入和偏差预警。

第一版包含：

- 加载 `Airport1.glb`。
- 根据 `zone_definitions.json` 自动判断构件所属区域。
- 根据构件名称归入固定展示分类。
- 根据 `schedule_data.json` 生成板块级计划时间。
- 拖动时间轴，让模型按板块整体变色。
- 点击构件时展示其所在板块信息，也可以点选构件信息，显示如属于E1 + 管桩
- 支持按区域、分类筛选查看。

第一版不包含：

- 实际完成率录入。
- 施工偏差分析。
- 前置/后置任务网络分析。
- 单构件级施工计划。
- Revit/Dynamo 预打标签流程。

## 4. 区域口径

系统只展示 19 个大区：

```text
E1, E2, E3, E4, E5, E6, E7, E8, E9, E10, E11, E12, E13
W1, W2, W3, W4, W5, W6
```

区域划分基于俯视平面，不考虑上下层、重叠区、跨区构件。

构件归区规则：

```text
构件 mesh -> 计算世界坐标包围盒 -> 取中心点 [x, z] -> 判断落入哪个区域多边形
```

如果构件中心点没有落入任何区域，则标记为 `unassigned`，进入审计报告，不静默丢弃。

当前已完成区域标定，结果文件为：

```text
zone_definitions.json
```

该文件可作为正式项目区域基准。只要后续模型仍使用同一 `Airport1.glb` 坐标系，该区域文件即可复用。

## 5. 分类口径

前端固定展示 8 个分类：

```text
灌注桩
管桩
咬合桩
钢板桩
立柱桩
钢围檩
混凝土支撑
钢支撑
```

GLB 构件名到展示分类的规则：

| GLB 名称或关键词 | 展示分类 |
|---|---|
| 灌注桩 | 灌注桩 |
| 管桩 | 管桩 |
| 立柱桩 | 立柱桩 |
| 格构柱、鱼珠站-格构柱 | 立柱桩 |
| 钢板桩 | 钢板桩 |
| 素桩、荤桩、850mm@600、单轴搅拌桩、旋喷桩、排桩 | 咬合桩 |
| 钢围檩、工字钢、JCHJ、H800x300 | 钢围檩 |
| 板支撑、混凝土支撑 | 混凝土支撑 |
| 1000x1000、800x800、1200x800、1200x1000、1000x800、1300x2700、600x600、1500x1000、300x1500 | 钢支撑 |

未能匹配规则的构件归入 `未分类`，进入审计报告。

## 6. 计划口径

官方进度计划中主要任务类型为：

```text
工程桩
支护桩
钢支撑
```

展示分类需要映射回官方计划组：

| 展示分类 | 官方计划组 |
|---|---|
| 灌注桩 | 工程桩 |
| 管桩 | 工程桩 |
| 立柱桩 | 工程桩 |
| 钢板桩 | 支护桩 |
| 咬合桩 | 支护桩 |
| 钢围檩 | 钢支撑 |
| 混凝土支撑 | 钢支撑 |
| 钢支撑 | 钢支撑 |

官方计划中的子区需要合并到大区：

| 官方区域 | 展示区域 |
|---|---|
| E7-1、E7-2、E7-3 | E7 |
| E8-1、E8-2 | E8 |
| E9-1、E9-2 | E9 |
| E10-1、E10-2、E10-3、E10-4 | E10 |
| E11-1、E11-2、E11-3、E11-4 | E11 |
| E12-1、E12-2、E12-3 | E12 |
| W3-1、W3-2、W3-3、W3-4 | W3 |
| 其他区域 | 保持原值 |

计划时间生成规则：

```text
同一大区 + 同一展示分类如果有多条来源任务：
start_date = 最早开始日期
end_date = 最晚结束日期
source_tasks = 保留全部来源任务
```

计划匹配等级：

```text
exact：任务名明确匹配到展示分类
coarse：从工程桩/支护桩/钢支撑粗分类继承
merged：由多个子区合并得到
coarse_merged：粗分类继承且发生子区合并
missing：模型存在板块，但计划中没有对应时间
```

## 7. 时间轴状态规则

第一版只展示计划状态：

```text
当前日期 < start_date -> 未开始
start_date <= 当前日期 <= end_date -> 施工中
当前日期 > end_date -> 计划完成
```

建议颜色：

| 状态 | 颜色 |
|---|---|
| 未开始 | 灰色 |
| 施工中 | 蓝色 |
| 计划完成 | 绿色 |
| 无计划 | 深灰或透明 |

滞后、预警、实际完成率不进入第一版。

## 8. 主要页面与交互

第一版页面应是一个施工进度工作台，而不是营销展示页。

推荐布局：

```text
顶部：项目名称、当前日期、模式提示
左侧：区域树、分类筛选、图例
中间：BIM 3D 模型视口
右侧：当前板块信息、来源计划、统计摘要
底部：时间轴
```

核心交互：

- 拖动时间轴，模型按板块状态变色。
- 勾选区域，只显示或高亮对应区域构件。
- 勾选分类，只显示或高亮对应类别。
- 点击构件，展示该构件所属板块：

```text
区域
分类
板块 ID
计划开始日期
计划结束日期
来源任务
匹配等级
板块构件数量
ElementID / 构件名称
```

## 9. 验收标准

第一版完成后应满足：

- 能加载 `Airport1.glb` 并稳定渲染。
- 能读取 `zone_definitions.json` 并自动归区。
- 能从官方计划生成 `block_schedule.json`。
- 能从模型和区域规则生成 `model_blocks.json`。
- 能将 `model_blocks` 与 `block_schedule` 对齐。
- 拖动时间轴时，模型按板块状态变色。
- 点击任意已归区构件时，能显示正确区域和分类。
- 未归区构件、无计划板块、计划无模型板块能被明确列入审计报告。

## 10. 后续扩展

第二阶段可加入：

- 板块级实际进度录入。
- 实际进度与计划进度对比。
- 滞后预警。
- 进度统计图表。
- 区域/分类维度的完成率。
- 与后端数据库持久化对接。

